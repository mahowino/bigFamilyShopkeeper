<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.bigfamilyshopkeeper">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BigFamilyShopkeeper"
        tools:targetApi="31">
        <activity
            android:name=".views.activities.viewGoodsToWithdraw"
            android:exported="false" />
        <activity
            android:name=".views.activities.MainPage"
            android:exported="false"
            android:label="@string/title_activity_main_page" />
        <activity
            android:name=".views.activities.MyCart"
            android:exported="false"
            android:label="my cart" />
        <activity
            android:name=".views.activities.PhoneNumberActivity"
            android:exported="false"
            android:label="my phone number" />
        <activity
            android:name=".views.activities.verifyOTP"
            android:exported="false"
            android:label="verify OTP" />
        <activity
            android:name=".views.activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>